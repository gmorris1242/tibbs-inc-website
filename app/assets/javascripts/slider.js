$(document).ready(function() {
  $('.slider').slick({
    infinite: true,
    slidesToShow: 1,
    slidesToScroll: 1,
    arrows: false,
    buttons: true,
    autoplay: true,
    autoplaySpeed: 3000
  });
});
